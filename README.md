# Fluid Simulation & Rendering Using SPH

## Build Instructions

### On Linux

From the root directory, run

```bash
mkdir build && cd build
cmake ../
make
```

Then, to run the project, run

```bash
    ./build/bin/SPH
```
